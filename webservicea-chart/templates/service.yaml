apiVersion: v1
kind: Service
metadata:
  name: {{ include "webservicea-chart.fullname" . }}
  labels:
    {{- include "webservicea-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webservicea-chart.selectorLabels" . | nindent 4 }}
